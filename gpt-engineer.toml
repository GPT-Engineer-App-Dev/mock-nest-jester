[project]
base_dir = "."           # base directory to operate in (for monorepos)
src_dir = "./src"        # source directory (under the base directory) from which context will be retrieved

[run]
build = "npm run build"
test = "npm run test"
lint = "quick-lint-js"

[gptengineer-app]
project_id = "0a81e5c7-69cf-49af-b805-d0975e3b3521"
openapi = []
